# This file is generated by `cargo hakari`.
# To regenerate, run:
#     cargo hakari generate

[package]
name = "workspace-hack"
version = "0.1.0"
description = "workspace-hack package, managed by hakari"
# You can choose to publish this crate: see https://docs.rs/cargo-hakari/latest/cargo_hakari/publishing.
publish = false

# The parts of the file between the BEGIN HAKARI SECTION and END HAKARI SECTION comments
# are managed by hakari.

### BEGIN HAKARI SECTION
[dependencies]
# cortex-m = { version = "0.7", default-features = false, features = ["critical-section-single-core"] }
embassy-executor = { version = "0.5", default-features = false, features = ["arch-cortex-m", "defmt", "executor-thread", "integrated-timers", "task-arena-size-32768"] }
embassy-time = { version = "0.3", default-features = false, features = ["defmt-timestamp-uptime"] }
embassy-time-driver = { version = "0.1", default-features = false, features = ["tick-hz-32_768"] }
embedded-hal = { version = "0.2", default-features = false, features = ["unproven"] }
futures-util = { version = "0.3", default-features = false, features = ["sink"] }
nb = { version = "0.1", default-features = false, features = ["unstable"] }
panic-probe = { version = "0.3", default-features = false, features = ["print-defmt"] }

[build-dependencies]
proc-macro2 = { version = "1" }
quote = { version = "1" }
syn = { version = "1", features = ["extra-traits", "full"] }

### END HAKARI SECTION
